import java.util.Arrays;

public class Test {

    public static void main(String[] args) {
        String test = "Check";
        byte[] bytes = test.getBytes();
        bytes.
        System.out.println();
        String format = String.format("These are the bytes: %s", bytes);
        System.out.println(format);
    }
}
